---
use_default_rules: true
skip_list:
  # 602 broken for strings, may be fixed with ansible cr #51030 (https://github.com/ansible/ansible/pull/51030)
  # will be there somewhere at ansible > 2.7.5
  - '602'
  - '405'
  - '106'  # role name pattern - sseide.knxd is official galaxy name...
